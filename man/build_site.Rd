% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_site.R
\name{build_site}
\alias{build_site}
\title{Build a GitHub Pages site for the publications index}
\usage{
build_site(
  dir = ".",
  docs = "docs",
  site_title = "Risk Management — Publications Index",
  theme = "jekyll-theme-cayman",
  author = "Francis Tsiboe, Ph.D.",
  scholar = "https://scholar.google.com/citations?user=ox2t_YIAAAAJ&hl=en",
  orcid = "https://orcid.org/0000-0001-5984-1072",
  linkedin = "https://www.linkedin.com/in/francis-tsiboe-02b97248/"
)
}
\arguments{
\item{dir}{Path to the publications directory containing the topic sub-folders
and \code{links.csv}.}

\item{docs}{Output directory for the site (GitHub Pages source). Default
\code{"docs"} (relative to the repository root).}

\item{site_title}{Title used in the page heading, front matter, and config.}

\item{theme}{A GitHub Pages supported Jekyll theme. Default
\code{"jekyll-theme-cayman"}.}

\item{author, scholar, orcid, linkedin}{Attribution shown on the page.}
}
\value{
Invisibly, the path to \code{docs}. Called for the side effect of writing
\code{index.md} (and \code{_config.yml} if absent).
}
\description{
Generates a single-page Jekyll site (\code{index.md} + \code{_config.yml})
under \code{docs/}, suitable for GitHub Pages "Deploy from a branch" using the
\code{/docs} folder on \code{main}. A normal \code{git push} then publishes the
site. The page lists every topic (with a jump-to table of contents and
per-topic counts) and each publication's public DOI/URL, sorted
newest-to-oldest; ARPC hub links are flagged with a dagger.
}
\details{
\code{_config.yml} is written only if it does not already exist, so a
customized theme or settings are preserved across rebuilds; \code{index.md} is
always regenerated.
}
\examples{
\dontrun{
build_site("data-raw/publications/risk-management")
}
}
