% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_wiki.R
\name{build_wiki}
\alias{build_wiki}
\title{Build a GitHub-wiki edition of the publications index}
\usage{
build_wiki(
  dir = ".",
  out_dir = file.path(dir, "wiki"),
  author = "Francis Tsiboe, Ph.D.",
  scholar = "https://scholar.google.com/citations?user=ox2t_YIAAAAJ&hl=en",
  orcid = "https://orcid.org/0000-0001-5984-1072",
  linkedin = "https://www.linkedin.com/in/francis-tsiboe-02b97248/"
)
}
\arguments{
\item{dir}{Path to the publications directory containing the topic sub-folders
and \code{links.csv}.}

\item{out_dir}{Directory to write the wiki pages to. Defaults to a \code{wiki/}
sub-folder of \code{dir}.}

\item{author, scholar, orcid, linkedin}{Attribution details shown on the Home
page.}
}
\value{
Invisibly, the path to \code{out_dir}. Called for the side effect of writing
\code{Home.md}, \code{_Sidebar.md}, and one Markdown page per topic.
}
\description{
Generates a multi-page GitHub wiki from a \code{links.csv} file (columns
\code{file}, \code{url}) and the topic sub-folders under \code{dir}. The wiki
comprises a \code{Home.md} landing page (intro, attribution, publication count,
and a linked topic list with per-topic counts), one page per topic (entries
sorted newest-to-oldest, each linking to its public DOI/URL), and a
\code{_Sidebar.md} for navigation. Entries whose URL is an ARPC hub page are
flagged with a dagger.
}
\details{
The wiki is a separate Git repository on GitHub
(\code{github.com/<user>/<repo>.wiki.git}); publish the generated pages by
copying them into a clone of that repository and pushing.
}
\examples{
\dontrun{
build_wiki("data-raw/publications/risk-management")
}
}
